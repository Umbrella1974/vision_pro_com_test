请写一个完全独立的 Python 脚本，用来测试 Apple Vision Pro 的 avp_stream 是否支持两个接收端同时连接。

重要限制：
1. 不要依赖我VisionPro_Teleop-main文件夹下里的任何代码。
2. 不要 import avp_leap.py、avp_client.py、leap_hand_utils、diffusion_policy、PyBullet、MuJoCo、机器人控制、Dynamixel、UR、RealSense 等任何项目内模块。
3. 这个脚本应该可以放在任意空目录里运行。
4. 唯一允许的非标准库依赖是 avp_stream。
5. 如果 avp_stream 没安装，请给出清晰报错提示：
       pip install avp_stream

请生成一个文件：

    test_avp_multi_receiver.py

功能目标：
同时启动两个独立的 receiver 进程，连接同一个 Vision Pro IP，检查 Vision Pro 是否能同时向两个客户端发送数据。

命令行参数：
- --ip：Vision Pro 的 IP，必填，例如 192.168.1.29
- --duration：测试时长，默认 60 秒
- --hz：每个 receiver 的打印频率，默认 5 Hz
- --record：是否把 VisionProStreamer(ip=..., record=...) 的 record 设置为 True，默认 False
- --mode：
    - single：只启动一个 receiver
    - dual：同一个 Python 脚本里用 multiprocessing 启动两个独立进程，默认 dual

核心逻辑：
1. 每个 receiver 进程都独立创建自己的 VisionProStreamer：
       vps = VisionProStreamer(ip=ip, record=record)
2. 循环读取：
       data = vps.latest
3. 每隔 1 / hz 秒打印一次状态：
   - receiver id，比如 A 或 B
   - elapsed time
   - loop count
   - receive FPS
   - data.keys()
   - right_wrist 是否存在
   - left_wrist 是否存在
   - right_fingers 的 shape，如果存在
   - left_fingers 的 shape，如果存在
   - 当前 time.time()
4. 增加数据新鲜度检测：
   - 维护上一次 right_wrist / left_wrist / right_fingers / left_fingers 的副本。
   - 如果连续多次读取到的数据没有变化，用 numpy.allclose 判断，打印：
         WARNING: data appears stale
   - 打印 stale_count。
5. 如果读取失败，不要立刻退出。捕获异常，打印异常类型和信息，然后继续尝试。
6. Ctrl+C 时两个进程都要优雅退出。
7. 输出格式要清晰，方便我比较 A 和 B 是否都在实时收到数据。
8. 不要写任何机器人控制逻辑。

请在文件开头写清楚使用方式：

安装依赖：
    pip install avp_stream numpy

运行双 receiver 测试：
    python test_avp_multi_receiver.py --ip 192.168.1.29 --duration 60

运行单 receiver 测试：
    python test_avp_multi_receiver.py --ip 192.168.1.29 --mode single --duration 60

判断标准：
- 如果 A 和 B 都持续打印数据，并且 FPS 正常、stale_count 不持续增加，说明 Vision Pro / avp_stream 可能支持两个独立接收端。
- 如果只有一个 receiver 有数据，另一个连接失败、卡住、FPS 极低，或 stale_count 持续增加，说明当前 Vision Pro app 或 avp_stream 可能不支持多个独立接收端。