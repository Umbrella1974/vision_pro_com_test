请写一个完全独立的 Python 脚本，用来测试 Apple Vision Pro 的 avp_stream 是否支持两个接收端同时连接。

文件名：

    test_avp_multi_receiver.py

重要限制：
1. 不要依赖任何现有项目代码。
2. 不要 import avp_leap.py、avp_client.py、leap_hand_utils、diffusion_policy、PyBullet、MuJoCo、机器人控制、Dynamixel、UR、RealSense 等模块。
3. 这个脚本应该可以放在任意空目录里运行。
4. 仅允许使用标准库、avp_stream、numpy。
5. 如果 avp_stream 或 numpy 没安装，请给出清晰报错提示：
       pip install avp_stream numpy
6. 需要兼容 Windows/macOS/Linux。特别注意 Windows multiprocessing 使用 spawn，所以 worker 函数必须定义在顶层，并且必须使用：
       if __name__ == "__main__":

实验目标：
同时启动两个独立 receiver 进程，连接同一个 Vision Pro IP，检查 Vision Pro 是否能同时向两个客户端发送数据。

命令行参数：
- --ip：Vision Pro 的 IP，必填，例如 192.168.1.29
- --duration：测试时长，默认 60 秒
- --hz：主进程打印频率，默认 5 Hz
- --record：是否把 VisionProStreamer(ip=..., record=...) 的 record 设置为 True，默认 False
- --mode：
    - single：只启动一个 receiver
    - dual：同一个 Python 脚本里用 multiprocessing 启动两个独立 receiver 进程，默认 dual

核心设计：
1. 每个 receiver 子进程都独立创建自己的 VisionProStreamer：
       vps = VisionProStreamer(ip=ip, record=record)
2. 子进程循环读取：
       data = vps.latest
3. 子进程不要直接 print，避免 A/B 输出交错。
4. 子进程把状态通过 multiprocessing.Queue 发回主进程。
5. 主进程统一打印 A/B 的状态，方便横向比较。
6. 父进程使用 multiprocessing.Event 通知子进程停止。
7. Ctrl+C 时，父进程设置 stop_event，然后 join 子进程；如果超时未退出，再 terminate。

状态信息要求：
每个 receiver 至少上报这些字段：
- receiver id，例如 A 或 B
- elapsed time
- poll_count：读取 vps.latest 的次数
- poll_fps：读取循环频率
- valid_count：data 是非空 dict 且包含至少一个有效字段的次数
- valid_fps：有效 data 的频率
- changed_count：检测到关键字段内容变化的次数
- changed_fps：关键字段内容变化的频率
- data.keys()
- right_wrist 是否存在
- left_wrist 是否存在
- right_fingers shape，如果存在
- left_fingers shape，如果存在
- last_error，如果有
- unchanged_count：连续多少次关键字段没变化
- local_receive_time：time.time()

关于 FPS 定义：
- poll_fps：本脚本读取 vps.latest 的循环频率，不代表真实新帧率。
- valid_fps：本脚本读到非空有效数据的频率。
- changed_fps：right_wrist / left_wrist / right_fingers / left_fingers 中至少一个字段发生数值变化的频率。
- 如果 avp_stream 的 data 中存在 timestamp、time、frame_id、frame、seq、counter 等源时间戳或帧号字段，请额外计算 source_new_frame_count 和 source_new_frame_fps。
- 如果没有源时间戳或帧号，不要把 unchanged 直接称为 stale，只打印：
      WARNING: data unchanged across reads
  因为用户手不动时，健康数据流也可能数值不变。

数据有效性判断：
- data 为 None：算 invalid，记录原因。
- data 不是 dict：算 invalid，记录类型。
- data 是空 dict：算 invalid。
- data 缺少 right_wrist、left_wrist、right_fingers、left_fingers 不要直接崩溃，记录缺失情况。
- 只要 data 是非空 dict 且包含任意 key，就可以计入 valid_count。
- right_wrist / left_wrist / right_fingers / left_fingers 的 shape 要安全获取，不能因为类型不同崩溃。

变化检测：
- 使用 numpy.asarray 转换关键字段。
- 使用 numpy.allclose 判断变化。
- 只有 shape 相同且 allclose 为 True 时才认为该字段未变化。
- shape 不同、字段从无到有、字段从有到无，都算变化。
- 变量名和打印文案不要使用 stale，除非确认存在源时间戳/帧号且源帧号长时间不变。
- 如果源时间戳/帧号存在且长期不变，可以打印：
      WARNING: source frame not advancing
- 如果只是关键字段数值没变化，只打印：
      WARNING: data unchanged across reads

record 参数说明：
- 默认 record=False。
- 如果用户传入 --record，则允许 VisionProStreamer 在当前目录产生记录文件。
- 在脚本开头注释里说明这一点。

输出格式：
- 主进程每隔 1 / hz 秒打印一次 A/B 状态摘要。
- dual 模式下，最好同一轮打印 A 和 B 两行，方便比较。
- 输出应包含明确的字段名，不要只打印裸列表。
- 如果某个 receiver 长时间没有状态上报，主进程打印：
      WARNING: receiver A/B has not reported recently

使用方式写在文件顶部注释里：

安装依赖：
    pip install avp_stream numpy

双 receiver 测试：
    python test_avp_multi_receiver.py --ip 192.168.1.29 --duration 60

单 receiver 测试：
    python test_avp_multi_receiver.py --ip 192.168.1.29 --mode single --duration 60

带 record 测试：
    python test_avp_multi_receiver.py --ip 192.168.1.29 --duration 60 --record

判断标准：
- 如果 A 和 B 都持续 reported，valid_fps 正常，并且如果存在源 timestamp/frame_id 时 source_new_frame_fps 正常，说明 Vision Pro / avp_stream 可能支持两个独立接收端。
- 如果只有一个 receiver 有有效数据，另一个连接失败、长期 None、空 dict、无状态上报，或源 frame_id/timestamp 不前进，说明当前 Vision Pro app 或 avp_stream 可能不支持多个独立接收端。
- 如果没有源 timestamp/frame_id，只能用 changed_fps 辅助判断；但用户手不动时 changed_fps 低不一定代表数据流异常。

请只输出完整 Python 文件内容。